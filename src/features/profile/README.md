# feature: profile

Perfiles públicos con stats y listas publicadas. UI (ProfileHeader, StatsGrid)
en Sprint 3; por ahora solo el tipo de dominio y el placeholder de página.

- `types/` — `PublicProfile`
